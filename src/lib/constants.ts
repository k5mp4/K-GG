// ---------------------------------------------------------------------------
// SDF / WebGL
// ---------------------------------------------------------------------------

/** ベジェ軸 SDF テクスチャのサイズ（幅・高さ共通） */
export const SDF_MAP_SIZE = 128;

/** ベジェ軸 SDF の再計算デバウンス遅延 (ms) */
export const SDF_DEBOUNCE_MS = 200;

// ---------------------------------------------------------------------------
// グラデーションランプ
// ---------------------------------------------------------------------------

/** グラデーションランプのテクスチャ幅（px）*/
export const RAMP_TEX_WIDTH = 256;

/** グラデーションランプ Canvas の横幅（px）*/
export const RAMP_W = 256;

/** グラデーションランプのバー高さ（px）*/
export const RAMP_BAR_H = 32;

/** グラデーションランプのハンドルエリア高さ（px）*/
export const RAMP_HANDLE_AREA = 18;

/** グラデーションランプのハンドル半幅（px）*/
export const RAMP_HANDLE_HALF = 6;

/** グラデーションランプのホイール操作ステップ（position 単位）*/
export const RAMP_WHEEL_STEP = 0.005;

// ---------------------------------------------------------------------------
// ベジェ軸エディタ
// ---------------------------------------------------------------------------

/** アンカーポイントの描画半径（px）*/
export const ANCHOR_RADIUS = 6;

/** コントロールポイントの描画半径（px）*/
export const CP_RADIUS = 4;

/** アンカー/コントロールポイントのヒット判定半径（px）*/
export const HIT_RADIUS = 16;

/** パスのヒット判定距離（px）*/
export const PATH_HIT_PX = 8;

/** パスのヒット判定サンプル数 */
export const PATH_SAMPLES = 50;

/** キャンバス外アンカー表示用パディング（px）*/
export const EDGE_PAD = 300;

// ---------------------------------------------------------------------------
// ベジェ SDF 生成
// ---------------------------------------------------------------------------

/** ベジェ曲線の距離計算サンプル数 */
export const BEZIER_SAMPLES = 64;

// ---------------------------------------------------------------------------
// 履歴 (Undo/Redo)
// ---------------------------------------------------------------------------

/** 履歴スタックの最大保持数 */
export const MAX_HISTORY = 50;

/** 履歴のデバウンス遅延 (ms) */
export const HISTORY_DEBOUNCE_MS = 400;
