{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "gradient-ramp-editor",
  "description": "Gradient Ramp editor window: state sync events with the main window only",
  "windows": ["gradient-ramp-editor"],
  "permissions": [
    "core:event:default",
    "core:window:allow-close"
  ]
}
